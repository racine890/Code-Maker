SetVar *platform PLATFORM
compare *platform win32
# On windows
if 0 then
	evaluate system('copy /Y "code_maker_backup.db" "data.db"')
# On linux
if 5 then
	evaluate system('cp "code_maker_backup.db" "data.db"')
MessageBox Done Recovered the last saved version !
end
